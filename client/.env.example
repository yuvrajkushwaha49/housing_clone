# Dev: use /api/v1 so phone/tablet on same Wi-Fi can access via your PC IP (Vite proxy).
# Production build: set full URL, e.g. https://yourdomain.com/api/v1
VITE_API_URL=/api/v1
VITE_APP_NAME=Workians
